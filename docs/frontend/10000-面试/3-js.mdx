### echarts的深入了解

### echarts怎么做自适应适配

### ES6的新特性

### promise 常用的有哪些方法，了解promise

### promise 的race 是什么

### async await 和foreach组合使用会有什么问题

### 解构赋值 变量重命名

### reduce 用法

### 理解的什么是闭包，反过来问下次调用这个闭包时返回值是最新的还是初始的值，如何做到的保留最新的值呢，这个值为什么会一直被保留下来不会被释放掉
闭包是指有权访问另外一个函数作用域中的变量的函数

特性：  
闭包可以访问当前函数以外的变量  
即使外部函数已经返回，闭包仍能访问外部函数定义的变量  
闭包可以更新外部变量的值  



### 什么是内存泄漏，或者说代码写着写着页面崩溃了，死循环为什么会导致页面崩溃

### 怎么用原生实现一个文件上传，input怎么清空值， input 在复用的时候怎么定唯一的标识（input中的值有自带的一个e可以做唯一标识）

### input 重复两次选择同一个文件会触发onchange事件吗，为什么

### 怎么生成一个10-100的随机数，怎么对数组进行从小到大的排序

### typeof [] 和 {}.toString() 输出的结果是什么

### 一下代码输出结果是什么
```javascript
{function (){
    var a=(b=5);
}}()
console.log(b);
```

```javascript
var fullname=1;
var obj={
    fullname:2,
    prop:{
        fullname:3,
        getFullname:function(){
            return this.fullname;
        }
    }
}
var test=obj.prop.getFullname;
console.log(obj.prop.getFullname());
console.log(test());
```

```javascript
console.log(false=="0");
console.log(false==="0")
```

```javascript
for(var i=0;i<4;i++){
    setTimeout(() => {
        console.log(i);
    }, 100);
}
```

### 写一个按照下面方式调用都能正常工作的sum方法
```javascript
console.log(sum(2,3));
console.log(sum(2)(3))
```

### 如何给Array对象添加自定义方法
https://juejin.cn/post/7007416743215759373

### 写个正则删除字符串中带中括号的内容
```javascript
"eee[31a]aaa[98c]".replace(正则,"")
```

### 宏任务微任务的打印结果题

### 怎么将一维数组转换成树型数组，树型数组转化成一维数组

### 怎么实现表格跨页选择