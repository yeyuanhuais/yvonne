import React from "react";
import Highlight from "./Highlight";
export default () => {
  return (
    <span
      style={{
        backgroundColor: color,
        borderRadius: "2px",
        color: "#fe0808",
        padding: "0.2rem",
      }}
    >
      ★
    </span>
  );
};
